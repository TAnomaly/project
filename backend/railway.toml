[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "./target/release/funify-backend"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
RUST_VERSION = "1.82.0"
RUST_LOG = "debug"